
# Environment
NODE_ENV=development

# HTTP
PORT=3000
HOST=0.0.0.0

# Database (Step 3+ will use this)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/url_shortener

# Redis (later steps)
REDIS_URL=redis://localhost:6379

# Kafka (later steps)
KAFKA_BROKERS=localhost:9092

# TimescaleDB (later steps)
TIMESCALE_URL=postgresql://postgres:postgres@localhost:5433/url_analytics

# Auth providers (later steps)
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
AUTH0_ISSUER=
AUTH0_AUDIENCE=
OIDC_ISSUER=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=

# JWT (Step 6)
JWT_ISSUER=url-shortener
JWT_AUDIENCE=url-shortener-web
JWT_ACCESS_TTL_SECONDS=900
JWT_SECRET=change-me-in-prod-32+chars
